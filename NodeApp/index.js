// const lib = require('./lib.js')
import { add, diff, div, mul } from './lib2.js';

console.log(add(5, 6), mul(2, 3), diff(5, 6), div(10, 2))

const a = 5;

// every file in node is module
// all property is protected my module

